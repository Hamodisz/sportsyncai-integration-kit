# SportSyncAI Identity API — example client.
# Contains ONLY the example script below; zero SportSyncAI internal code.
# Talks to the real hosted API over HTTPS, nothing local, nothing bundled.
FROM python:3.11-slim
WORKDIR /app
COPY example.py .
ENTRYPOINT ["python", "example.py"]
